[build-system]
requires = ["scikit-build-core>=0.10"]
build-backend = "scikit_build_core.build"

[project]
name = "downstream-tyr-user"
version = "0.0.0"
dependencies = ["pytyr"]

[tool.scikit-build]
wheel.packages = []
